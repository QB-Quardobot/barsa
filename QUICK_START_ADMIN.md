# 🚀 Быстрый запуск админ-панели

## Шаг 1: Установка зависимостей (если еще не установлены)

```bash
# Переходим в папку с ботом (если еще не там)
cd bot/barcelona_bots

# Активируем виртуальное окружение
source .venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

## Шаг 2: Запуск API сервера

**ВАЖНО:** Вы должны быть в папке `bot/barcelona_bots` и виртуальное окружение должно быть активировано!

```bash
# Проверьте, что вы в правильной папке
pwd
# Должно показать: /Users/who/barselona/bot/barcelona_bots

# Проверьте, что виртуальное окружение активировано
# В начале строки должно быть (.venv)
# Если нет - активируйте:
source .venv/bin/activate

# Теперь запускайте API сервер
python3 api_server.py
```

Должно появиться:
```
Starting API server on 0.0.0.0:8000
API Server started and database tables created
```

## Шаг 3: Запуск фронтенда (в другом терминале)

```bash
# Из корневой папки проекта (barselona)
npm run dev
```

## Шаг 4: Открыть админку

Откройте в браузере: **http://localhost:4321/admin**

---

## Проверка работы

1. **API работает:** http://localhost:8000/health → должно вернуть `{"status": "ok"}`
2. **Админка работает:** http://localhost:4321/admin → должна загрузиться темная панель

---

## Частые ошибки

### "ModuleNotFoundError: No module named 'email_validator'"

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### "cd: no such file or directory: bot/barcelona_bots"

**Причина:** Вы уже находитесь в папке `bot/barcelona_bots`

**Решение:** Просто выполните:
```bash
source .venv/bin/activate
python3 api_server.py
```

### "source: no such file or directory: .venv/bin/activate"

**Причина:** Виртуальное окружение не создано или вы не в правильной папке

**Решение:**
```bash
# Проверьте, что вы в правильной папке
pwd
# Должно быть: .../bot/barcelona_bots

# Проверьте наличие .venv
ls -la | grep venv

# Если .venv нет, создайте его:
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
