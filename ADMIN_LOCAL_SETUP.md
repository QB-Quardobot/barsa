# 🚀 Запуск админ-панели на локальной машине

## Быстрый старт

### Вариант 1: Запуск через main.py (рекомендуется)

Этот вариант запустит и Telegram ботов, и API сервер одновременно:

```bash
# Переходим в папку с ботом
cd bot/barcelona_bots

# Активируем виртуальное окружение
source .venv/bin/activate

# Устанавливаем зависимости (если еще не установлены)
pip install -r requirements.txt

# Запускаем ботов + API сервер
python3 main.py
```

API сервер будет доступен на `http://localhost:8000`

### Вариант 2: Запуск только API сервера (без ботов)

Если нужно только API для админки:

```bash
# Переходим в папку с ботом
cd bot/barcelona_bots

# Активируем виртуальное окружение
source .venv/bin/activate

# Запускаем только API сервер
python3 api_server.py
```

API сервер будет доступен на `http://localhost:8000`

---

## Запуск фронтенда (Astro)

В **отдельном терминале**:

```bash
# Из корневой папки проекта
npm run dev
```

Astro dev сервер будет доступен на `http://localhost:4321`

---

## Открытие админ-панели

После запуска обоих серверов откройте в браузере:

- **Главная панель:** http://localhost:4321/admin
- **Лиды:** http://localhost:4321/admin/leads
- **Пользователи:** http://localhost:4321/admin/users

---

## Проверка работы

1. **API сервер работает:** Откройте http://localhost:8000/health
   - Должен вернуть: `{"status": "ok", "timestamp": "..."}`

2. **Админка работает:** Откройте http://localhost:4321/admin
   - Должна загрузиться страница с темным дизайном и статистикой

---

## Возможные проблемы

### CORS ошибки

Если видите ошибки CORS в консоли браузера, убедитесь что:
- API сервер запущен на `http://localhost:8000`
- В `api_server.py` настроен CORS (уже настроен для `allow_origins=["*"]`)

### Данные не загружаются

1. Проверьте, что API сервер запущен: `curl http://localhost:8000/health`
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Убедитесь, что база данных создана и содержит данные

### Порт занят

Если порт 8000 занят, измените его в `api_server.py`:
```python
run_api_server(host="0.0.0.0", port=8001)  # Используйте другой порт
```

И обновите `API_BASE` в админ-страницах или используйте переменную окружения:
```bash
export PUBLIC_API_BASE=http://localhost:8001/api
npm run dev
```

### Python не найден

На macOS используйте `python3` вместо `python`:
```bash
# Правильно
python3 api_server.py

# Неправильно
python api_server.py  # Может не работать на macOS
```

---

## Переменные окружения (опционально)

Можно настроить API URL через переменную окружения:

```bash
# В .env файле или перед запуском
export PUBLIC_API_BASE=http://localhost:8000/api
npm run dev
```
